import { Tags, CalendarPlus, Clock3, Headphones, ArrowUpRight } from 'lucide-react';
const scenarios = [{ icon: Tags, label: 'Ask about pricing', detail: 'Customer asks about pricing', query: 'How much are your services?' }, { icon: CalendarPlus, label: 'Book an appointment', detail: 'Customer books an appointment', query: 'I’d like to book a gel manicure.' }, { icon: Clock3, label: 'Check opening hours', detail: 'Customer asks opening hours', query: 'What are your opening hours?' }, { icon: Headphones, label: 'Talk to staff', detail: 'Customer requests human support', query: 'Can I talk to staff?' }];
export function ScenarioCards({ onScenario }: { onScenario: (q: string) => void }) { return <section className="scenarios" id="how-it-works"><div className="section-line"><h2>Explore common requests</h2><span>A little inspiration to get started</span></div><div className="scenario-grid">{scenarios.map(({icon: Icon, ...s}, i) => <button key={s.label} onClick={() => onScenario(s.query)}><div className="scenario-top"><Icon size={18}/><span>0{i + 1}<ArrowUpRight size={14}/></span></div><h3>{s.label}</h3><p>{s.detail}</p></button>)}</div></section>; }
