import { AudioLines, CalendarDays, MessageSquare, Headphones } from 'lucide-react';
export function Hero() { return <section className="hero"><div className="eyebrow"><span/> KHAIFROST AI CORE <span/></div><h1>Your AI receptionist.<br/><span>Always available.</span></h1><p>Answer questions, guide customers and book appointments —<br className="desktop-break"/> 24/7, without missing an opportunity.</p><div className="hero-features">{[[AudioLines,'Voice'],[MessageSquare,'Chat'],[CalendarDays,'Booking'],[Headphones,'Customer Support']].map(([Icon,label]) => { const I = Icon as typeof AudioLines; return <span key={String(label)}><I size={13}/>{String(label)}</span>; })}</div></section>; }
